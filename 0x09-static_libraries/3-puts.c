#include "main.h"
#include <stdio.h>

void _puts(char *str);

int main(void)
{
    _puts("\"Programming is like building a multilingual puzzle");
    return (0);
}

void _puts(char *str)
{
    puts(str);
}
